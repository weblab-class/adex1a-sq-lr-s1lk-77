import React from "react";

type Props = {
  // TO DO
};

const InventoryBar = (props: Props) => {
  return (
    <div>
      <p className="text-lg"> Inventory Bar </p>
    </div>
  );
};

export default InventoryBar;
